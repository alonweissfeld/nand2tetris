/**
 * Initializes a new Direction Game and start running it.
 */
class Main {
    function void main() {
        var DirectionGame game;
        let game = DirectionGame.new();

        do game.run();
        do game.dispose();

        return;
    }
}
